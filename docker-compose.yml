services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: daftra_financial_intelligence
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - daftra_pg:/var/lib/postgresql/data
volumes:
  daftra_pg:
